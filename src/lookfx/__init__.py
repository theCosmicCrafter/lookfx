"""lookfx — standalone stills+video FX app over the vendored engines."""
# Single source of the release version: pyproject reads it via
# [tool.setuptools.dynamic]; bump only here (and CHANGELOG.md).
__version__ = "0.1.2"
